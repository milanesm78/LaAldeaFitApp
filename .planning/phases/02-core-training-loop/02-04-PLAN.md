---
phase: 02-core-training-loop
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/features/workouts/components/workout-history-list.tsx
  - src/features/workouts/components/session-detail-card.tsx
  - src/pages/client/WorkoutHistoryPage.tsx
  - src/layouts/ClientLayout.tsx
  - src/features/dashboard/components/client-list-table.tsx
  - src/features/dashboard/components/client-status-badge.tsx
  - src/features/dashboard/components/client-search-bar.tsx
  - src/features/dashboard/components/client-detail-tabs.tsx
  - src/pages/trainer/ClientsPage.tsx
  - src/pages/trainer/ClientDetailPage.tsx
  - src/pages/trainer/ClientPlanPage.tsx
  - src/App.tsx
  - src/locales/en/translation.json
  - src/locales/es/translation.json
autonomous: false
requirements:
  - WLOG-04
  - DASH-01
  - DASH-02
  - DASH-03
  - EXER-03

must_haves:
  truths:
    - "Client can view their workout history showing past sessions with all logged sets and session duration"
    - "Trainer can view a scrollable list of all clients with activity status and search bar"
    - "Trainer sees color-coded indicators showing which clients completed today's workout"
    - "Trainer can select a client and view their plan, logs, and workout history in a tabbed detail view"
    - "ClientPlanPage.tsx from Plan 02 is deleted and its functionality lives inside ClientDetailTabs Plan tab"
  artifacts:
    - path: "src/pages/client/WorkoutHistoryPage.tsx"
      provides: "Client workout history page with past sessions"
      min_lines: 20
    - path: "src/features/dashboard/components/client-list-table.tsx"
      provides: "Trainer client list with status indicators"
      min_lines: 30
    - path: "src/features/dashboard/components/client-detail-tabs.tsx"
      provides: "Tabbed client detail view (plan, logs) consolidating ClientPlanPage functionality"
      min_lines: 40
    - path: "src/pages/trainer/ClientDetailPage.tsx"
      provides: "Trainer view of individual client with tabs, replaces ClientPlanPage"
      min_lines: 30
  key_links:
    - from: "src/pages/client/WorkoutHistoryPage.tsx"
      to: "src/features/workouts/hooks/useWorkouts.ts"
      via: "useWorkoutHistory for past sessions"
      pattern: "useWorkoutHistory"
    - from: "src/features/dashboard/components/client-list-table.tsx"
      to: "src/features/dashboard/hooks/useDashboard.ts"
      via: "useClientDashboard for client list with status"
      pattern: "useClientDashboard"
    - from: "src/pages/trainer/ClientDetailPage.tsx"
      to: "src/features/dashboard/hooks/useDashboard.ts"
      via: "useClientDetail for client plan and logs"
      pattern: "useClientDetail"
    - from: "src/features/dashboard/components/client-detail-tabs.tsx"
      to: "src/features/plans/hooks/usePlans.ts"
      via: "useActivePlan and PlanVersionBanner consolidated from ClientPlanPage"
      pattern: "useActivePlan"
---

<objective>
Build the workout history view for clients and the enhanced trainer dashboard with client status indicators, search, and drill-down client detail with tabbed plan/logs view. End with a human verification checkpoint for the complete Phase 2 training loop.

Purpose: Workout history gives clients visibility into their progress (motivation). The trainer dashboard is how Javier monitors 20-50 clients efficiently -- color-coded indicators at a glance, search for quick access, drill-down for details.
Output: Workout history page, trainer client list with status, client detail with tabbed view, end-to-end verification
</objective>

<execution_context>
@/Users/macbookpro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-training-loop/02-RESEARCH.md
@.planning/phases/02-core-training-loop/02-01-SUMMARY.md
@.planning/phases/02-core-training-loop/02-02-SUMMARY.md
@.planning/phases/02-core-training-loop/02-03-SUMMARY.md
@src/features/workouts/hooks/useWorkouts.ts
@src/features/dashboard/hooks/useDashboard.ts
@src/features/dashboard/types.ts
@src/pages/trainer/ClientPlanPage.tsx
@src/App.tsx
@src/layouts/ClientLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client workout history page with session detail cards and client nav update</name>
  <files>
src/features/workouts/components/session-detail-card.tsx
src/features/workouts/components/workout-history-list.tsx
src/pages/client/WorkoutHistoryPage.tsx
src/layouts/ClientLayout.tsx
src/locales/en/translation.json
src/locales/es/translation.json
  </files>
  <action>
**Create `src/features/workouts/components/session-detail-card.tsx`:**
Card showing a single past workout session.
- Props: session (SessionWithSets including training day label, all sets grouped by exercise)
- Shows: date (formatted with date-fns format), training day label, session duration (using formatSessionDuration)
- Expandable: tap to show all exercises with their logged sets (set_number, weight_kg, reps)
- Compact mobile-friendly layout

**Create `src/features/workouts/components/workout-history-list.tsx`:**
Scrollable list of past workout sessions.
- Props: sessions (SessionWithSets[])
- Maps over sessions rendering SessionDetailCard for each
- Empty state: "No workouts logged yet. Start your first workout from your training plan."
- Per discretion: simple date list (not calendar view) -- simplest useful approach
- Shows most recent sessions first

**Create `src/pages/client/WorkoutHistoryPage.tsx`:**
Page for client to view their workout history.
- Route: /client/history
- Uses useAuth() to get user ID, then useWorkoutHistory(userId)
- Loading state with skeleton cards
- Renders WorkoutHistoryList with the fetched sessions
- Default: last 20 sessions (load more button if needed)

**Update `src/layouts/ClientLayout.tsx`:**
- Add "History" nav item (History icon from lucide-react) pointing to /client/history
- Final nav: Home, My Plan, History

**Update translation files (`src/locales/en/translation.json` and `src/locales/es/translation.json`):**
Add workout history translation keys:
- history.title, history.noWorkouts, history.noWorkoutsDescription, history.loadMore, history.sessionOn, history.duration, history.setsCompleted
  </action>
  <verify>
Run `pnpm build` -- must pass with zero TypeScript errors. Verify: (1) session-detail-card.tsx uses formatSessionDuration, (2) WorkoutHistoryPage.tsx uses useWorkoutHistory hook, (3) ClientLayout.tsx has 3 nav items (Home, My Plan, History), (4) history translation keys present in both EN and ES files.
  </verify>
  <done>Client can navigate to /client/history from bottom nav, see a list of past workout sessions with dates and durations, and expand each session to view all logged sets with exercise names and values.</done>
</task>

<task type="auto">
  <name>Task 2: Create trainer dashboard with client status, search, and tabbed client detail (replacing ClientPlanPage)</name>
  <files>
src/features/dashboard/components/client-status-badge.tsx
src/features/dashboard/components/client-search-bar.tsx
src/features/dashboard/components/client-list-table.tsx
src/features/dashboard/components/client-detail-tabs.tsx
src/pages/trainer/ClientsPage.tsx
src/pages/trainer/ClientDetailPage.tsx
src/pages/trainer/ClientPlanPage.tsx
src/App.tsx
src/locales/en/translation.json
src/locales/es/translation.json
  </files>
  <action>
**Create `src/features/dashboard/components/client-status-badge.tsx`:**
Color-coded status indicator for client activity.
- Props: status ('active' | 'inactive' | 'pending'), todayWorkoutStatus ('completed' | 'in_progress' | 'not_started' | null)
- Activity status badge: green background for active, amber/yellow for pending, gray for inactive
- Today's workout indicator (small dot or icon): green circle for completed, amber circle for in_progress, gray circle for not_started
- Uses shadcn/ui Badge component or custom styled span

**Create `src/features/dashboard/components/client-search-bar.tsx`:**
Search input for filtering the client list.
- Props: value (string), onChange (string => void), placeholder
- Uses shadcn/ui Input with Search icon (lucide-react)
- Filters client-side (20-50 clients fits in memory per locked decision)

**Create `src/features/dashboard/components/client-list-table.tsx`:**
Scrollable list of all clients with status indicators.
- Props: clients (ClientDashboardRow[]), searchQuery (string), onClientClick (clientId => void)
- Filters clients by searchQuery (case-insensitive match on full_name or email)
- Each row: client name, email (truncated on mobile), ClientStatusBadge, last workout date (relative: "2 days ago" using formatRelativeDate)
- Tap on row navigates to client detail page
- Responsive: on mobile show name + status badge only; on desktop show all columns
- Keep existing activation toggle from TrainerDashboard.tsx (integrate or preserve)

**Create `src/features/dashboard/components/client-detail-tabs.tsx`:**
Tabbed view of a specific client's data for the trainer. This component MUST consolidate all functionality that was in ClientPlanPage.tsx (from Plan 02):
- Props: clientId (string)
- Tab 1: "Plan" -- consolidate the full ClientPlanPage functionality here:
  - Fetch active plan via useActivePlan(clientId)
  - If no active plan: show "No plan assigned" with "Create Plan" button linking to /trainer/clients/:clientId/plan/new
  - If active plan exists: render PlanCard with plan details
  - If draft version exists: show PlanVersionBanner at top of this tab
  - "Edit Plan" button: calls useCreatePlanVersion(activePlanId), then navigates to /trainer/clients/:clientId/plan/:newPlanId/edit
  - "Create Plan" button: navigates to /trainer/clients/:clientId/plan/new
- Tab 2: "Logs" -- shows client's recent workout sessions (reuse WorkoutHistoryList from Task 1)
- Each tab lazy-loads data via TanStack Query (only fetches when tab is selected)
- Uses shadcn/ui Tabs component

**Delete `src/pages/trainer/ClientPlanPage.tsx`:**
This file was created by Plan 02 at route /trainer/clients/:clientId. Its functionality (active plan view, PlanVersionBanner, version activation, Create Plan button) is now fully consolidated into the "Plan" tab of ClientDetailTabs above. DELETE this file entirely -- do NOT leave it as an orphan.

**Create `src/pages/trainer/ClientDetailPage.tsx`:**
Trainer's detail view of a specific client, replacing ClientPlanPage at the same route.
- Route: /trainer/clients/:clientId
- Shows client name and email at top
- Renders ClientDetailTabs for the tabbed plan/logs view
- Back button to return to client list

**Update `src/pages/trainer/ClientsPage.tsx`:**
Enhance the ClientsPage created by Plan 02 to use the dashboard data layer instead of simple profile queries.
- Must use useClientDashboard() hook (not the old simple profile query from TrainerDashboard)
- Search bar at top (ClientSearchBar)
- Client list below (ClientListTable)
- Navigate to /trainer/clients/:clientId on row click
- Handle loading and error states

**Update `src/App.tsx`:**
- Add client route: /client/history -> WorkoutHistoryPage (inside ClientLayout)
- Update trainer route: /trainer/clients/:clientId -> ClientDetailPage (replace the ClientPlanPage import)
- Remove the ClientPlanPage import entirely
- Keep all other existing routes unchanged

**Update translation files (`src/locales/en/translation.json` and `src/locales/es/translation.json`):**
Add dashboard and client detail translation keys:
- dashboard.searchClients, dashboard.searchPlaceholder, dashboard.noClients, dashboard.clientCount, dashboard.todayStatus
- clientDetail.plan, clientDetail.logs, clientDetail.editPlan, clientDetail.noPlan
- status.today.completed, status.today.inProgress, status.today.notStarted
  </action>
  <verify>
Run `pnpm build` -- must pass with zero TypeScript errors. Verify: (1) ClientPlanPage.tsx no longer exists: `! test -f src/pages/trainer/ClientPlanPage.tsx`, (2) ClientDetailPage.tsx exists and imports ClientDetailTabs, (3) App.tsx imports ClientDetailPage (not ClientPlanPage) for /trainer/clients/:clientId, (4) client-detail-tabs.tsx uses useActivePlan and PlanVersionBanner in the Plan tab, (5) all dashboard translation keys present in both EN and ES.
  </verify>
  <done>Trainer can view all clients with color-coded status indicators and search bar, drill into any client to see Plan tab (with full version management from old ClientPlanPage) and Logs tab. ClientPlanPage.tsx is deleted with zero orphan references. Route /trainer/clients/:clientId now serves ClientDetailPage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 2 training loop end-to-end</name>
  <files>no files modified -- verification only</files>
  <action>
Human verification checkpoint for the complete Phase 2 core training loop. All code was implemented in Plans 02-01 through 02-04 Tasks 1-2. This task pauses execution for the user to verify the end-to-end flow works correctly across all roles and features.
  </action>
  <verify>
User performs the verification steps listed in how-to-verify below and reports results.
  </verify>
  <done>User has approved the complete Phase 2 training loop by confirming all 9 verification areas pass.</done>
  <what-built>
Complete core training loop: trainer plan builder with versioning, client plan view with YouTube video embed, workout session logging with pre-fill and stepper controls, workout history, and trainer dashboard with client status indicators and drill-down.
  </what-built>
  <how-to-verify>
**Prerequisites:** Run the Phase 2 SQL migration in Supabase SQL Editor (supabase/migrations/00002_training_loop_schema.sql). Start the dev server with `pnpm dev`.

**1. Trainer Plan Builder (login as trainer):**
1. Navigate to /trainer/clients -- verify the client list loads with search bar
2. Click on a client -- verify client detail page shows tabbed view with "No plan assigned" in Plan tab
3. Click "Create Plan" -- verify the plan builder form loads
4. Enter plan name "Week 1 Push/Pull", set cycle length to 4 weeks
5. Verify Day A is auto-created. Add Day B.
6. In Day A, click "Add Exercise" -- verify the exercise picker dialog opens with your exercise library
7. Select an exercise -- verify it appears with sets/reps/weight fields
8. Add 2-3 exercises to Day A with different sets/reps/weight values
9. Add 1-2 exercises to Day B
10. Save the plan -- verify success toast and redirect to client detail page
11. Verify the plan shows as "Draft" with an "Activate" button in the Plan tab
12. Click "Activate" -- verify confirmation dialog, then activation success

**2. Plan Versioning:**
1. On the client detail page Plan tab, click "Edit Plan"
2. Verify a new draft version is created (version 2)
3. Make a change (add an exercise, change reps)
4. Save and activate -- verify version 2 is now active, version 1 is archived

**3. Client Plan View (login as an active client):**
1. Navigate to /client/plan -- verify the training plan loads with day tabs
2. Switch between Day A and Day B tabs
3. Verify exercises show with prescribed sets/reps/weight
4. Tap an exercise -- verify YouTube video loads (lite embed, click to play)

**4. Workout Logging:**
1. On Day A tab, click "Start Workout"
2. Verify the workout page loads with exercises and set rows
3. Verify set values are pre-filled (prescribed values for first session)
4. Use stepper buttons (+/-) to adjust weight and reps
5. Tap the log button for each set -- verify it turns green
6. Verify session timer is running
7. Click "Finish Workout" -- verify completion and summary screen
8. Verify the summary shows duration and sets/exercises completed

**5. Workout History:**
1. Navigate to /client/history -- verify the completed session appears
2. Expand the session -- verify all logged sets are visible with correct values

**6. Pre-fill from Last Session:**
1. Start another workout for the same training day
2. Verify set values are pre-filled from the PREVIOUS session's actual logged values (not the prescribed values)

**7. Trainer Dashboard:**
1. Login as trainer, go to /trainer/clients
2. Verify the client who just worked out shows "completed" status (green indicator)
3. Search for a client by name -- verify the filter works
4. Click on the client -- verify tabbed detail view with Plan and Logs tabs
5. Verify Logs tab shows the client's recent workout sessions

**8. Mobile UX:**
1. Test on a mobile viewport (Chrome DevTools device toolbar, iPhone 12/14)
2. Verify: touch targets are large enough, set stepper buttons are easily tappable, bottom navigation works, plan tabs are scrollable if needed

**9. Bilingual:**
1. Toggle language to Spanish
2. Verify all Phase 2 UI text appears in Spanish (plan builder labels, workout logging, dashboard, history)
  </how-to-verify>
  <resume-signal>Type "approved" if the complete training loop works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- ClientPlanPage.tsx is deleted -- no orphan file or import
- Client workout history shows past sessions with all logged sets
- Trainer dashboard shows client list with color-coded status indicators and search
- Trainer can drill into client detail with tabbed plan/logs view (Plan tab has all old ClientPlanPage functionality)
- End-to-end flow verified: create plan -> activate -> client views -> logs workout -> history visible -> trainer sees status
</verification>

<success_criteria>
- Client can view workout history with past sessions, logged sets, and duration (WLOG-04)
- Trainer sees all clients with activity status (DASH-01)
- Color-coded today's workout completion indicators visible (DASH-02)
- Trainer can drill into any client's plan and logs (DASH-03)
- YouTube video embed works from client workout view (EXER-03 confirmation)
- Search bar filters clients efficiently for 20-50 client roster (locked decision)
- All Phase 2 UI is bilingual (Spanish/English)
- ClientPlanPage.tsx deleted, route served by ClientDetailPage with no broken imports
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-training-loop/02-04-SUMMARY.md`
</output>

---
phase: 02-core-training-loop
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/youtube.ts
  - src/features/exercises/components/ExerciseForm.tsx
  - src/features/exercises/components/ExerciseCard.tsx
  - src/features/workouts/components/youtube-player.tsx
  - src/features/plans/components/training-day-tab.tsx
autonomous: true
requirements:
  - EXER-03
gap_closure: true

must_haves:
  truths:
    - "Tapping video icon on exercise in client plan view expands collapsible section showing YouTube demo video"
    - "Exercise form rejects YouTube URLs that cannot produce a playable video ID"
    - "If a stored youtube_url somehow fails video ID extraction, user sees a fallback message instead of blank space"
  artifacts:
    - path: "src/lib/utils/youtube.ts"
      provides: "Single source of truth for YouTube URL parsing and validation"
      exports: ["extractYouTubeVideoId", "getYouTubeThumbnailUrl", "isValidYouTubeUrl"]
      contains: "shorts"
    - path: "src/features/workouts/components/youtube-player.tsx"
      provides: "YouTube player with fallback UI when video ID extraction fails"
      min_lines: 20
    - path: "src/features/exercises/components/ExerciseForm.tsx"
      provides: "Exercise form using shared extractYouTubeVideoId for validation"
      contains: "import.*youtube"
  key_links:
    - from: "src/features/exercises/components/ExerciseForm.tsx"
      to: "src/lib/utils/youtube.ts"
      via: "import { extractYouTubeVideoId, isValidYouTubeUrl } for form validation"
      pattern: "import.*from.*youtube"
    - from: "src/features/exercises/components/ExerciseCard.tsx"
      to: "src/lib/utils/youtube.ts"
      via: "import { extractYouTubeVideoId } for thumbnail extraction"
      pattern: "import.*from.*youtube"
    - from: "src/features/workouts/components/youtube-player.tsx"
      to: "src/lib/utils/youtube.ts"
      via: "extractYouTubeVideoId to parse URL before rendering"
      pattern: "extractYouTubeVideoId"
    - from: "src/features/plans/components/training-day-tab.tsx"
      to: "src/features/workouts/components/youtube-player.tsx"
      via: "YouTubePlayer renders video inside CollapsibleContent"
      pattern: "YouTubePlayer"
---

<objective>
Fix the YouTube exercise video not showing in the client plan view (UAT Test 8 failure). The root cause is a mismatch between the permissive ExerciseForm URL validation and the strict regex-based extractYouTubeVideoId parser -- URLs that pass form validation fail at render time, causing YouTubePlayer to return null and the CollapsibleContent to appear empty.

Purpose: This is a major UAT gap -- the core "watch exercise video" feature does not work for clients viewing their training plan.
Output: Unified YouTube URL parsing across all components, stricter form validation that prevents unrenderable URLs, and a visible fallback if extraction ever fails at render time.
</objective>

<execution_context>
@/Users/macbookpro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/exercise-video-not-showing.md
@src/lib/utils/youtube.ts
@src/features/exercises/components/ExerciseForm.tsx
@src/features/exercises/components/ExerciseCard.tsx
@src/features/workouts/components/youtube-player.tsx
@src/features/plans/components/training-day-tab.tsx
@src/features/plans/components/exercise-picker-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify YouTube URL parsing into single shared module and fix ExerciseForm/ExerciseCard</name>
  <files>
src/lib/utils/youtube.ts
src/features/exercises/components/ExerciseForm.tsx
src/features/exercises/components/ExerciseCard.tsx
  </files>
  <action>
**Rewrite `src/lib/utils/youtube.ts` extractYouTubeVideoId:**

Replace the strict 11-char regex approach with a robust URL()-based parser (same strategy as ExerciseForm's local function, but complete). The new implementation must:
1. Use `new URL(url)` for proper parsing (wrapped in try/catch returning null)
2. Handle ALL these hostname variations: `youtube.com`, `www.youtube.com`, `m.youtube.com`, `youtu.be`
3. Handle ALL these path formats:
   - `/watch?v=VIDEO_ID` (standard watch URL, extract from `v` query param)
   - `/embed/VIDEO_ID` (embed URL)
   - `/shorts/VIDEO_ID` (Shorts URL -- CURRENTLY MISSING from the regex version)
   - `/v/VIDEO_ID` (old embed format)
   - `/VIDEO_ID` (youtu.be short link -- extract from pathname)
4. Do NOT enforce exactly 11 characters. YouTube IDs are typically 11 chars but the parser should accept `[a-zA-Z0-9_-]+` (at least 1 char). The video ID is whatever the URL says it is.
5. Strip any trailing path segments or query params from the extracted ID (e.g., `/embed/VIDEO_ID?autoplay=1` should extract just VIDEO_ID)
6. Keep the existing `getYouTubeThumbnailUrl` function unchanged.

**Add `isValidYouTubeUrl` to `src/lib/utils/youtube.ts` as a NEW export:**

```typescript
export function isValidYouTubeUrl(url: string): boolean {
  return extractYouTubeVideoId(url) !== null;
}
```

This ties validation directly to extractability -- if we can extract a video ID, the URL is valid. No more mismatch.

**Update `src/features/exercises/components/ExerciseForm.tsx`:**
1. DELETE the local `extractYouTubeVideoId` function (lines 17-43)
2. DELETE the local `isValidYouTubeUrl` function (lines 45-58)
3. ADD import: `import { extractYouTubeVideoId, isValidYouTubeUrl } from "@/lib/utils/youtube";`
4. The rest of the component stays the same -- it already uses `extractYouTubeVideoId(youtubeUrl)` for the thumbnail preview (line 93) and calls `isValidYouTubeUrl` in validate() (line 102). The function signatures are compatible.

**Update `src/features/exercises/components/ExerciseCard.tsx`:**
1. DELETE the local `extractYouTubeVideoId` function (lines 10-34)
2. ADD import: `import { extractYouTubeVideoId } from "@/lib/utils/youtube";`
3. The rest of the component stays the same -- it already uses `extractYouTubeVideoId(exercise.youtube_url)` for the thumbnail (line 44).
  </action>
  <verify>
Run `pnpm build` -- must pass with zero TypeScript errors. Then verify:
1. `grep -c "function extractYouTubeVideoId" src/features/exercises/components/ExerciseForm.tsx` returns 0 (local function deleted)
2. `grep -c "function extractYouTubeVideoId" src/features/exercises/components/ExerciseCard.tsx` returns 0 (local function deleted)
3. `grep -c "function isValidYouTubeUrl" src/features/exercises/components/ExerciseForm.tsx` returns 0 (local function deleted)
4. `grep "import.*youtube" src/features/exercises/components/ExerciseForm.tsx` shows import from @/lib/utils/youtube
5. `grep "import.*youtube" src/features/exercises/components/ExerciseCard.tsx` shows import from @/lib/utils/youtube
6. `grep "shorts" src/lib/utils/youtube.ts` confirms /shorts/ support is present
7. `grep "export function isValidYouTubeUrl" src/lib/utils/youtube.ts` confirms new export exists
  </verify>
  <done>Single extractYouTubeVideoId in src/lib/utils/youtube.ts handles all YouTube URL formats (including /shorts/). ExerciseForm and ExerciseCard use the shared module instead of local copies. Form validation uses isValidYouTubeUrl which is tied to extractability -- any URL that passes validation will produce a playable video.</done>
</task>

<task type="auto">
  <name>Task 2: Add YouTubePlayer fallback UI and verify end-to-end video rendering</name>
  <files>
src/features/workouts/components/youtube-player.tsx
src/features/plans/components/training-day-tab.tsx
  </files>
  <action>
**Update `src/features/workouts/components/youtube-player.tsx`:**

Instead of returning `null` when extractYouTubeVideoId fails (causing invisible empty space inside CollapsibleContent), show a visible fallback:

```typescript
import { useTranslation } from "react-i18next";
import LiteYouTubeEmbed from "react-lite-youtube-embed";
import "react-lite-youtube-embed/dist/LiteYouTubeEmbed.css";
import { extractYouTubeVideoId } from "@/lib/utils/youtube";

interface YouTubePlayerProps {
  youtubeUrl: string;
  exerciseName: string;
}

export function YouTubePlayer({ youtubeUrl, exerciseName }: YouTubePlayerProps) {
  const { t } = useTranslation();
  const videoId = extractYouTubeVideoId(youtubeUrl);

  if (!videoId) {
    return (
      <div className="w-full aspect-video rounded-lg overflow-hidden bg-muted flex items-center justify-center">
        <p className="text-sm text-muted-foreground px-4 text-center">
          {t("plan.videoUnavailable", "Video unavailable")}
        </p>
      </div>
    );
  }

  return (
    <div className="w-full aspect-video rounded-lg overflow-hidden">
      <LiteYouTubeEmbed
        id={videoId}
        title={exerciseName}
        poster="hqdefault"
        noCookie={true}
      />
    </div>
  );
}
```

This ensures that if a URL somehow passes validation but still fails extraction (e.g., data was entered before the fix), the user sees "Video unavailable" instead of blank space.

**No changes to `src/features/plans/components/training-day-tab.tsx`:**

The training-day-tab.tsx component is already correctly structured -- it wraps YouTubePlayer inside CollapsibleContent and gates on `pe.exercises.youtube_url`. With the fixed extractYouTubeVideoId now properly extracting IDs from all URL formats, YouTubePlayer will render the video. No code changes needed to this file.

**Add i18n keys for the fallback message.** Update both translation files:
- English (`src/locales/en/translation.json`): Add `"plan.videoUnavailable": "Video unavailable"`
- Spanish (`src/locales/es/translation.json`): Add `"plan.videoUnavailable": "Video no disponible"`

Note: Only add these keys if they do not already exist. Merge into the existing `plan` section of each translation file.
  </action>
  <verify>
Run `pnpm build` -- must pass with zero TypeScript errors. Then verify:
1. `grep "videoUnavailable" src/features/workouts/components/youtube-player.tsx` confirms fallback UI exists
2. `grep "videoUnavailable" src/locales/en/translation.json` confirms English i18n key
3. `grep "videoUnavailable" src/locales/es/translation.json` confirms Spanish i18n key
4. `grep -c "return null" src/features/workouts/components/youtube-player.tsx` returns 0 (no more silent null return)

Final manual check: Start `pnpm dev`, navigate to client plan view (/client/plan), tap an exercise with a youtube_url. The collapsible section should expand showing the YouTube lite embed player.
  </verify>
  <done>YouTubePlayer shows a visible "Video unavailable" fallback instead of returning null. Combined with Task 1's parser fix, tapping the video icon on an exercise in the client plan view now correctly shows the YouTube demo video (UAT Test 8 resolved).</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero TypeScript errors
- Only ONE extractYouTubeVideoId function exists in the codebase (in src/lib/utils/youtube.ts) -- no local copies in ExerciseForm.tsx or ExerciseCard.tsx
- isValidYouTubeUrl is exported from src/lib/utils/youtube.ts and used in ExerciseForm.tsx validation
- YouTubePlayer never returns null -- shows fallback UI instead
- YouTube /shorts/ URLs are handled by the parser
- Client plan view: tapping exercise row expands collapsible with visible YouTube video embed
</verification>

<success_criteria>
- UAT Test 8 passes: tapping video icon on exercise expands collapsible section showing YouTube demo video
- All YouTube URL formats work: /watch?v=, /embed/, /shorts/, /v/, youtu.be/
- ExerciseForm rejects URLs where no video ID can be extracted (validation tied to extraction)
- No duplicate extractYouTubeVideoId implementations across codebase
- Fallback UI visible when video extraction fails (not blank space)
- Build passes, all existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-training-loop/02-05-SUMMARY.md`
</output>
